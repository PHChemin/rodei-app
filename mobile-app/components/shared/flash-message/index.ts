import FlashMessageProvider from "./flash-message-provider";
import FlashMessageRenderer from "./flash-message-renderer";

const FlashMessage = {
  Provider: FlashMessageProvider,
  Renderer: FlashMessageRenderer,
};

export default FlashMessage;
